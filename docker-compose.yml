services:
  cli-wechat-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["./scripts/start-bridge.sh"]
    env_file:
      - ./.env
    user: "0:0"
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./home:/root
    networks:
      - openclaw-net

networks:
  openclaw-net:
    external: true
